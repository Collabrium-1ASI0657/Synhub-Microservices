# =============================================
# JWT CONFIGURATION
# =============================================

# Secret key for JWT token signing and validation
# Must be at least 32 characters long for security
# Must be the same as the one in IAM-SERVICE
JWT_SECRET=your_super_secure_jwt_secret_key_min_32_chars